# 📦 راهنمای کامل ساخت فایل نصبی WebP Converter

## 🎯 خلاصه سریع

برای ساخت فایل نصبی، کافیست:

```bash
# 1. نصب وابستگی‌ها (فقط بار اول)
npm install

# 2. آماده‌سازی آیکون (توضیحات در پایین)
# فایل icon.ico را در ریشه پروژه قرار دهید

# 3. ساخت فایل نصبی
npm run build
```

یا می‌توانید فایل `build.bat` را دابل‌کلیک کنید!

---

## 📋 پیش‌نیازها

### 1. نصب Node.js
- از [nodejs.org](https://nodejs.org/) نسخه LTS را دانلود و نصب کنید
- برای بررسی نصب صحیح:
  ```bash
  node --version
  npm --version
  ```

### 2. نصب بسته‌های پروژه
```bash
cd c:\Users\Legend\Desktop\webp-converter
npm install
```

این دستور تمام وابستگی‌های لازم از جمله:
- Electron
- electron-builder
- Sharp (برای پردازش تصویر)
- و سایر ماژول‌ها

را نصب می‌کند.

---

## 🎨 آماده‌سازی آیکون

برنامه به یک فایل `icon.ico` برای نمایش در ویندوز نیاز دارد.

### روش 1: استفاده از ابزار آنلاین (پیشنهادی)

1. **آماده کردن تصویر اولیه**
   - یک تصویر PNG با ابعاد 256x256 یا 512x512 پیکسل آماده کنید
   - می‌توانید از فایل `icon.svg` موجود در پروژه استفاده کنید

2. **تبدیل SVG به PNG** (اگر از icon.svg استفاده می‌کنید)
   - به سایت [CloudConvert](https://cloudconvert.com/svg-to-png) بروید
   - فایل `icon.svg` را آپلود کنید
   - ابعاد را روی 512x512 تنظیم کنید
   - دانلود PNG

3. **تبدیل PNG به ICO**
   - به یکی از سایت‌های زیر بروید:
     - [Convertio](https://convertio.co/png-ico/)
     - [ICO Convert](https://icoconvert.com/)
     - [AnyConv](https://anyconv.com/png-to-ico-converter/)
   - فایل PNG را آپلود کنید
   - گزینه "Multi-size" را انتخاب کنید (شامل 16, 32, 48, 64, 128, 256)
   - فایل ICO را دانلود کنید

4. **قرار دادن در پروژه**
   ```bash
   # فایل icon.ico را در ریشه پروژه کپی کنید
   c:\Users\Legend\Desktop\webp-converter\icon.ico
   ```

### روش 2: استفاده از ImageMagick (برای کاربران پیشرفته)

اگر ImageMagick نصب دارید:

```bash
# تبدیل SVG به PNG
magick convert icon.svg -resize 512x512 icon.png

# تبدیل PNG به ICO با چند اندازه
magick convert icon.png -define icon:auto-resize=256,128,96,64,48,32,16 icon.ico
```

### روش 3: استفاده از GIMP (نرم‌افزار رایگان)

1. [GIMP](https://www.gimp.org/) را دانلود و نصب کنید
2. فایل SVG یا PNG را باز کنید
3. Image → Scale Image → 256x256
4. File → Export As → انتخاب فرمت ICO
5. "Compressed (PNG)" را انتخاب کنید
6. تمام اندازه‌ها را علامت بزنید

---

## 🔨 ساخت فایل نصبی

### روش 1: استفاده از فایل BAT (ساده‌ترین روش)

فقط کافیست فایل `build.bat` را دابل‌کلیک کنید!

این فایل:
- ✅ وجود Node.js را بررسی می‌کند
- ✅ وابستگی‌ها را نصب می‌کند (در صورت نیاز)
- ✅ وجود آیکون را چک می‌کند
- ✅ فایل نصبی را می‌سازد
- ✅ اطلاعات کامل نمایش می‌دهد

### روش 2: استفاده از خط فرمان

```bash
# ساخت فایل نصبی استاندارد (64-bit)
npm run build

# ساخت نسخه قابل حمل (بدون نیاز به نصب)
npm run build-portable

# ساخت برای هر دو معماری (32 و 64 بیت)
npm run build-all
```

### مدت زمان ساخت

- اولین بار: 5-10 دقیقه (شامل دانلود Electron binaries)
- دفعات بعدی: 2-5 دقیقه

---

## 📁 فایل‌های خروجی

پس از ساخت موفق، فایل‌های زیر در پوشه `dist` ایجاد می‌شوند:

```
dist/
├── WebP Converter-Setup-1.0.0.exe    ← فایل نصبی اصلی (حدود 100-150 MB)
├── win-unpacked/                     ← فایل‌های برنامه (برای تست)
└── builder-debug.yml                 ← اطلاعات debug
```

### انواع فایل‌های قابل ساخت:

1. **NSIS Installer** (پیش‌فرض)
   - فایل: `WebP Converter-Setup-1.0.0.exe`
   - قابل نصب با wizard
   - ایجاد میانبر در Start Menu و Desktop
   - شامل uninstaller

2. **Portable Version**
   - فایل: `WebP Converter-Portable-1.0.0.exe`
   - بدون نیاز به نصب
   - قابل اجرا از USB

3. **Win-Unpacked**
   - پوشه: `dist/win-unpacked/`
   - نسخه فشرده‌نشده
   - برای تست سریع

---

## ✅ تست فایل نصبی

### مرحله 1: نصب

1. به پوشه `dist` بروید
2. فایل `WebP Converter-Setup-1.0.0.exe` را اجرا کنید
3. **هشدار Windows SmartScreen**:
   - اگر این پیغام را دیدید: "Windows protected your PC"
   - روی "More info" کلیک کنید
   - سپس "Run anyway" را بزنید
   - این طبیعی است چون برنامه با code certificate امضا نشده

4. مراحل نصب را طی کنید:
   - انتخاب مسیر نصب
   - گزینه‌های میانبر
   - تایید نصب

### مرحله 2: اجرا و تست

1. برنامه را از Start Menu یا Desktop اجرا کنید
2. عملکردهای زیر را تست کنید:
   - ✅ انتخاب فایل‌های تصویری
   - ✅ تنظیم کیفیت
   - ✅ تبدیل به WebP
   - ✅ ذخیره فایل‌ها

### مرحله 3: حذف نصب (اختیاری)

1. Control Panel → Programs → Uninstall a program
2. "WebP Converter" را پیدا کنید
3. کلیک راست → Uninstall

---

## 🚀 توزیع و انتشار

### آماده‌سازی برای انتشار

1. **تست روی سیستم‌های مختلف**
   - Windows 10 (64-bit)
   - Windows 11
   - سیستم‌های با آنتی‌ویروس مختلف

2. **محاسبه Checksum** (برای امنیت)
   ```bash
   # PowerShell
   Get-FileHash "dist\WebP Converter-Setup-1.0.0.exe" -Algorithm SHA256
   ```

3. **فایل README برای کاربران**
   - نحوه نصب
   - راهنمای استفاده
   - لینک دانلود

### روش‌های انتشار

#### 1. GitHub Releases (پیشنهادی)

```bash
# ساخت Release جدید در GitHub
git tag -a v1.0.0 -m "نسخه اول"
git push origin v1.0.0
```

سپس در GitHub:
- به بخش Releases بروید
- Create a new release
- فایل‌های installer را آپلود کنید
- توضیحات و checksum را اضافه کنید

#### 2. سایر روش‌ها

- **Google Drive / Dropbox**: آپلود و اشتراک‌گذاری لینک
- **سرور شخصی**: آپلود روی سایت خودتان
- **Microsoft Store**: (نیاز به حساب توسعه‌دهنده)

---

## 🛠️ عیب‌یابی

### خطا: "Application entry file does not exist"

**راه‌حل:**
```bash
# بررسی وجود فایل main.js
dir main.js

# اگر وجود ندارد، از git بازیابی کنید یا دوباره ایجاد کنید
```

### خطا: "icon.ico not found"

**راه‌حل:**
- مطابق راهنمای بالا، فایل `icon.ico` را ایجاد کنید
- یا موقتاً خط `"icon": "icon.ico"` را از `package.json` حذف کنید

### خطا: مربوط به Sharp module

```bash
# Rebuild کردن Sharp
npm rebuild sharp

# یا نصب مجدد
npm uninstall sharp
npm install sharp
```

### خطا: "Cannot find module electron"

```bash
# نصب مجدد وابستگی‌ها
rm -rf node_modules
npm install
```

### حجم فایل نصبی خیلی زیاد است

این **طبیعی** است! دلایل:
- Electron شامل Chromium و Node.js است
- ماژول Sharp شامل کتابخانه‌های native
- معمولاً: 100-200 MB

برای کاهش حجم:
- از فشرده‌سازی UPX استفاده کنید (مستلزم تنظیمات اضافی)
- فایل‌های غیرضروری را exclude کنید

### خطا در هنگام اجرا: "DLL not found"

**راه‌حل:**
- مطمئن شوید Sharp به درستی نصب شده
- بسته Visual C++ Redistributable را نصب کنید

---

## 📊 تنظیمات پیشرفته

### تغییر نسخه

در `package.json`:
```json
{
  "version": "1.0.0"  ← تغییر دهید
}
```

### تغییر نام برنامه

```json
{
  "name": "webp-converter",
  "build": {
    "productName": "WebP Converter"  ← نام نمایشی
  }
}
```

### افزودن فایل‌های اضافی به installer

```json
{
  "build": {
    "extraFiles": [
      "docs/**",
      "samples/**"
    ]
  }
}
```

### Code Signing (امضای دیجیتال)

برای حذف هشدار SmartScreen، نیاز به گواهی دیجیتال دارید:

1. خرید Code Signing Certificate (حدود $200-400/سال)
2. تنظیم در `package.json`:

```json
{
  "build": {
    "win": {
      "certificateFile": "cert.pfx",
      "certificatePassword": "your-password"
    }
  }
}
```

---

## 📞 پشتیبانی

اگر مشکلی پیش آمد:

1. **لاگ‌ها را بررسی کنید**
   ```bash
   npm run build 2>&1 | tee build.log
   ```

2. **بررسی نسخه‌ها**
   ```bash
   node --version    # باید v18+ باشد
   npm --version     # باید v9+ باشد
   ```

3. **پاک کردن cache**
   ```bash
   npm cache clean --force
   rm -rf node_modules
   rm package-lock.json
   npm install
   ```

---

## 🎉 موفق باشید!

حالا برنامه شما آماده توزیع است. 

**نکات پایانی:**
- ✅ همیشه قبل از انتشار، تست کامل انجام دهید
- ✅ نسخه‌بندی را رعایت کنید (Semantic Versioning)
- ✅ Changelog بنویسید
- ✅ از کاربران بازخورد بگیرید

---

**ساخته شده با ❤️ برای تبدیل آسان تصاویر به WebP**
