# راهنمای کامل ایجاد Release در GitHub

این فایل شامل تمام مراحل لازم برای ایجاد یک Release حرفه‌ای در GitHub است.

## مرحله 1: آماده‌سازی تغییرات

ابتدا تمام تغییرات جدید را commit و push کنید:

```bash
# اضافه کردن تمام فایل‌های جدید
git add .

# Commit تغییرات
git commit -m "feat: add installer build configuration and documentation"

# Push به GitHub
git push origin main
```

## مرحله 2: ساخت فایل نصبی

### اطمینان از نصب وابستگی‌ها:

```bash
npm install
```

### ساخت Installer:

```bash
# ساخت فایل نصبی برای ویندوز
npm run build
```

یا دابل‌کلیک روی `build.bat`

**خروجی**: فایل `WebP Converter-Setup-1.0.0.exe` در پوشه `dist`

## مرحله 3: ایجاد Git Tag

Tag ها برای مشخص کردن نسخه‌های مختلف استفاده می‌شوند:

```bash
# ایجاد tag با نام v1.0.0
git tag -a v1.0.0 -m "Release version 1.0.0 - نسخه اول WebP Converter"

# مشاهده tag ها
git tag

# Push کردن tag به GitHub
git push origin v1.0.0
```

## مرحله 4: ایجاد Release در GitHub

### روش 1: از طریق وب (پیشنهادی)

1. به repository خود در GitHub بروید:
   ```
   https://github.com/Mahdeep/webp-converter
   ```

2. روی تب **"Releases"** کلیک کنید (سمت راست صفحه)

3. روی دکمه **"Draft a new release"** کلیک کنید

4. **اطلاعات Release را پر کنید:**

   - **Choose a tag**: `v1.0.0` را انتخاب کنید (باید قبلاً push شده باشد)
   - **Release title**: `WebP Converter v1.0.0`
   - **Description**: متن زیر را کپی کنید:

```markdown
# 🎉 WebP Converter v1.0.0

## 📥 دانلود

فایل نصبی را از بخش Assets پایین دانلود کنید:
- **WebP Converter-Setup-1.0.0.exe** - فایل نصبی برای ویندوز (64-bit)

## ✨ ویژگی‌ها

این نسخه اول WebP Converter با قابلیت‌های زیر منتشر شد:

- ✅ تبدیل دسته‌جمعی تصاویر به فرمت WebP
- ✅ پشتیبانی از فرمت‌های JPG, PNG, BMP, TIFF, GIF
- ✅ کنترل دقیق کیفیت خروجی (1-100)
- ✅ نمایش میزان کاهش حجم فایل‌ها
- ✅ رابط کاربری فارسی و ساده
- ✅ نمایش پیشرفت تبدیل به صورت زنده

## 📦 نصب

1. فایل `WebP Converter-Setup-1.0.0.exe` را دانلود کنید
2. فایل را اجرا کنید
3. مراحل نصب را طی کنید
4. از Start Menu یا Desktop برنامه را اجرا کنید

### ⚠️ هشدار Windows SmartScreen

اگر این پیغام را دیدید: **"Windows protected your PC"**
- روی **"More info"** کلیک کنید
- سپس **"Run anyway"** را بزنید
- این طبیعی است چون برنامه با code certificate امضا نشده

## 📊 اطلاعات فنی

- **نسخه**: 1.0.0
- **تاریخ انتشار**: 25 نوامبر 2025
- **پلتفرم**: Windows 10/11 (64-bit)
- **حجم**: ~100-150 MB
- **فریمورک**: Electron 28
- **پردازش تصویر**: Sharp 0.33

## 🐛 گزارش مشکلات

اگر مشکلی پیش آمد، در بخش [Issues](https://github.com/Mahdeep/webp-converter/issues) گزارش دهید.

## 📝 Changelog

### نسخه 1.0.0 (2025-11-25)

#### افزوده شده:
- پشتیبانی کامل از تبدیل تصاویر به WebP
- رابط گرافیکی با Electron
- انتخاب چندگانه فایل‌ها
- کنترل کیفیت خروجی
- نمایش آمار کاهش حجم
- فایل نصبی NSIS برای ویندوز
- مستندات کامل فارسی

---

**محاسبه Checksum (SHA256):**
```
(در اینجا checksum فایل نصبی را قرار دهید)
```

برای محاسبه:
```powershell
Get-FileHash "WebP Converter-Setup-1.0.0.exe" -Algorithm SHA256
```
```

5. **آپلود فایل نصبی:**
   - به بخش **"Attach binaries"** بروید
   - فایل `WebP Converter-Setup-1.0.0.exe` را از پوشه `dist` drag & drop کنید
   - منتظر بمانید تا آپلود کامل شود

6. **انتشار Release:**
   - اگر آماده انتشار هستید: **"Publish release"**
   - اگر می‌خواهید بعداً منتشر کنید: **"Save draft"**

### روش 2: از طریق GitHub CLI (برای کاربران پیشرفته)

اگر GitHub CLI نصب دارید:

```bash
# ایجاد Release
gh release create v1.0.0 \
  --title "WebP Converter v1.0.0" \
  --notes-file RELEASE_NOTES.md \
  "dist/WebP Converter-Setup-1.0.0.exe"
```

## مرحله 5: تأیید و تست

پس از انتشار Release:

1. **لینک Release را بررسی کنید:**
   ```
   https://github.com/Mahdeep/webp-converter/releases/tag/v1.0.0
   ```

2. **فایل نصبی را دانلود و تست کنید**

3. **README.md را به‌روز کنید:**
   - لینک دانلود را اضافه کنید
   - شماره نسخه را به‌روز کنید

## نکات مهم

### 🔐 امنیت

- **Checksum را حتماً اضافه کنید** تا کاربران بتوانند اصالت فایل را تأیید کنند
- برای محاسبه SHA256:
  ```powershell
  Get-FileHash "dist\WebP Converter-Setup-1.0.0.exe" -Algorithm SHA256
  ```

### 📝 مستندات

فایل‌های زیر را در Release ذکر کنید:
- لینک به راهنمای نصب: `INSTALL_GUIDE_FA.md`
- لینک به مستندات: `README.md`
- لیست تغییرات (Changelog)

### 🏷️ نسخه‌بندی (Versioning)

از Semantic Versioning استفاده کنید:
- **Major.Minor.Patch** (مثلاً 1.0.0)
- **Major**: تغییرات breaking
- **Minor**: ویژگی‌های جدید (backward compatible)
- **Patch**: رفع باگ

### 📢 اطلاع‌رسانی

پس از انتشار:
- در README.md لینک دانلود را اضافه کنید
- در social media اعلام کنید
- به کاربران ایمیل بزنید (اگر دارید)

## مثال کامل دستورات

```bash
# 1. Commit تغییرات
git add .
git commit -m "feat: add installer and documentation for v1.0.0"
git push origin main

# 2. ایجاد و push کردن tag
git tag -a v1.0.0 -m "Release v1.0.0"
git push origin v1.0.0

# 3. ساخت فایل نصبی
npm run build

# 4. محاسبه checksum
Get-FileHash "dist\WebP Converter-Setup-1.0.0.exe" -Algorithm SHA256

# 5. سپس از وب GitHub Release را ایجاد کنید
```

## عیب‌یابی

### Tag قبلاً وجود دارد

```bash
# حذف tag از local
git tag -d v1.0.0

# حذف tag از remote (با احتیاط!)
git push origin :refs/tags/v1.0.0

# ایجاد مجدد tag
git tag -a v1.0.0 -m "Release v1.0.0"
git push origin v1.0.0
```

### فایل نصبی خیلی بزرگ است

GitHub حداکثر 2GB برای هر فایل مجاز است. اگر فایل بزرگ‌تر است:
- از GitHub Large File Storage (LFS) استفاده کنید
- یا فایل را در جای دیگری (Google Drive) آپلود کنید و لینک دهید

### نمی‌توانم Release را edit کنم

- به صفحه Release بروید
- روی سه نقطه کنار Release کلیک کنید
- "Edit release" را انتخاب کنید

## نسخه‌های بعدی

برای نسخه‌های آینده:

```bash
# نسخه 1.1.0 (minor update)
git tag -a v1.1.0 -m "Release v1.1.0 - New features"

# نسخه 1.0.1 (patch/bugfix)
git tag -a v1.0.1 -m "Release v1.0.1 - Bug fixes"

# نسخه 2.0.0 (major update)
git tag -a v2.0.0 -m "Release v2.0.0 - Breaking changes"
```

## منابع

- [GitHub Docs: Creating releases](https://docs.github.com/en/repositories/releasing-projects-on-github/managing-releases-in-a-repository)
- [Semantic Versioning](https://semver.org/)
- [electron-builder Documentation](https://www.electron.build/)

---

**موفق باشید! 🚀**
